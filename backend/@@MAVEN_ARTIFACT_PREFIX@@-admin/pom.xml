<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>@@MAVEN_ARTIFACT_PREFIX@@</artifactId>
        <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
        <version>3.9.2</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-admin</artifactId>

    <description>
        web服务入口
    </description>

    <dependencies>

        <!-- spring-boot-devtools -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <optional>true</optional> <!-- 表示依赖不会传递 -->
        </dependency>

        <!-- spring-doc -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

         <!-- Mysql驱动包 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>

        <!-- 核心模块-->
        <dependency>
            <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
            <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-framework</artifactId>
        </dependency>

        <!-- 定时任务-->
        <dependency>
            <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
            <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-quartz</artifactId>
        </dependency>

        <!-- 代码生成-->
        <dependency>
            <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
            <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-generator</artifactId>
        </dependency>

        <!-- 业务模块。新业务代码统一放这里，别散在 @@MAVEN_ARTIFACT_PREFIX@@-system 里 -->
        <dependency>
            <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
            <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-biz</artifactId>
        </dependency>

        <!--
          MinIO 对象存储（**可选**）。不需要对象存储的项目：**删掉这一条 dependency 即可**，
          其余一行都不用动（FileStorageService 接口与本地实现无条件在
          @@MAVEN_ARTIFACT_PREFIX@@-common 里）。同时把 application.yml 的 file.storage.type 保持 local。
        -->
        <dependency>
            <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
            <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-storage-minio</artifactId>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <addResources>true</addResources>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>   
                <groupId>org.apache.maven.plugins</groupId>   
                <artifactId>maven-war-plugin</artifactId>   
                <version>3.1.0</version>   
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                    <warName>${project.artifactId}</warName>
                </configuration>   
           </plugin>   
        </plugins>
        <finalName>${project.artifactId}</finalName>
    </build>

</project>