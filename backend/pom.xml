<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	
    <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
    <artifactId>@@MAVEN_ARTIFACT_PREFIX@@</artifactId>
    <version>3.9.2</version>

    <name>@@MAVEN_ARTIFACT_PREFIX@@</name>
    <description>@@PROJECT_NAME@@</description>
    
    <properties>
        <app.version>3.9.2</app.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>17</java.version>
        <spring-boot.version>3.5.16</spring-boot.version>
        <mybatis-spring-boot.version>3.0.5</mybatis-spring-boot.version>
        <druid.version>1.2.28</druid.version>
        <yauaa.version>8.2.0</yauaa.version>
        <kaptcha.version>2.3.3</kaptcha.version>
        <pagehelper.boot.version>2.1.1</pagehelper.boot.version>
        <fastjson.version>2.0.64</fastjson.version>
        <oshi.version>7.4.4</oshi.version>
        <commons.io.version>2.22.0</commons.io.version>
        <poi.version>5.5.1</poi.version>
        <velocity.version>2.3</velocity.version>
        <jwt.version>0.9.1</jwt.version>
        <jaxb-api.version>2.3.1</jaxb-api.version>
        <springdoc.version>2.9.0</springdoc.version>
        <minio.version>8.5.17</minio.version>
    </properties>

    <!-- 依赖声明 -->
    <dependencyManagement>
        <dependencies>

            <!-- SpringBoot的依赖配置 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- 阿里数据库连接池 -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-3-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- 解析客户端操作系统、浏览器等 -->
            <dependency>
                <groupId>nl.basjes.parse.useragent</groupId>
                <artifactId>yauaa</artifactId>
                <version>${yauaa.version}</version>
            </dependency>

            <!-- pagehelper 分页插件 -->
            <dependency>
                <groupId>com.github.pagehelper</groupId>
                <artifactId>pagehelper-spring-boot-starter</artifactId>
                <version>${pagehelper.boot.version}</version>
            </dependency>

            <dependency>
                <groupId>org.mybatis.spring.boot</groupId>
                <artifactId>mybatis-spring-boot-starter</artifactId>
                <version>${mybatis-spring-boot.version}</version>
            </dependency>

            <dependency>
                <groupId>javax.xml.bind</groupId>
                <artifactId>jaxb-api</artifactId>
                <version>${jaxb-api.version}</version>
            </dependency>

            <!-- 获取系统信息 -->
            <dependency>
                <groupId>com.github.oshi</groupId>
                <artifactId>oshi-core</artifactId>
                <version>${oshi.version}</version>
            </dependency>

            <!-- spring-doc -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- io常用工具类 -->
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons.io.version}</version>
            </dependency>

            <!-- excel工具 -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <!-- velocity代码生成使用模板 -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity.version}</version>
            </dependency>

            <!-- 阿里JSON解析器 -->
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson.version}</version>
            </dependency>

            <!-- Token生成与解析-->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt</artifactId>
                <version>${jwt.version}</version>
            </dependency>

            <!-- 验证码 -->
            <dependency>
                <groupId>pro.fessional</groupId>
                <artifactId>kaptcha</artifactId>
                <version>${kaptcha.version}</version>
            </dependency>

            <!-- 定时任务-->
            <dependency>
                <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
                <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-quartz</artifactId>
                <version>${app.version}</version>
            </dependency>

            <!-- 代码生成-->
            <dependency>
                <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
                <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-generator</artifactId>
                <version>${app.version}</version>
            </dependency>

            <!-- 核心模块-->
            <dependency>
                <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
                <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-framework</artifactId>
                <version>${app.version}</version>
            </dependency>

            <!-- 系统模块-->
            <dependency>
                <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
                <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-system</artifactId>
                <version>${app.version}</version>
            </dependency>

            <!-- 通用工具-->
            <dependency>
                <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
                <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-common</artifactId>
                <version>${app.version}</version>
            </dependency>

            <!-- 业务模块。生成的 CRUD 默认落在这里（见 @@MAVEN_ARTIFACT_PREFIX@@-generator 的 generator.yml） -->
            <dependency>
                <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
                <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-biz</artifactId>
                <version>${app.version}</version>
            </dependency>

            <!--
              MinIO 对象存储 —— **可选**。只有 @@MAVEN_ARTIFACT_PREFIX@@-storage-minio 用得到。
              不需要对象存储的项目：把 @@MAVEN_ARTIFACT_PREFIX@@-admin/pom.xml 里对本模块的
              dependency 删掉即可，这个 SDK 也就不会被下载。
              版本已核实：8.5.x 是当前稳定线（Maven Central 另有 9.0.3，但很新）。
            -->
            <dependency>
                <groupId>io.minio</groupId>
                <artifactId>minio</artifactId>
                <version>${minio.version}</version>
            </dependency>

            <!-- 可选存储模块：对象存储实现（本地实现无条件在 @@MAVEN_ARTIFACT_PREFIX@@-common 里） -->
            <dependency>
                <groupId>@@JAVA_PACKAGE_ROOT@@</groupId>
                <artifactId>@@MAVEN_ARTIFACT_PREFIX@@-storage-minio</artifactId>
                <version>${app.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <modules>
        <module>@@MAVEN_ARTIFACT_PREFIX@@-admin</module>
        <module>@@MAVEN_ARTIFACT_PREFIX@@-framework</module>
        <module>@@MAVEN_ARTIFACT_PREFIX@@-system</module>
        <module>@@MAVEN_ARTIFACT_PREFIX@@-biz</module>
        <module>@@MAVEN_ARTIFACT_PREFIX@@-storage-minio</module>
        <module>@@MAVEN_ARTIFACT_PREFIX@@-quartz</module>
        <module>@@MAVEN_ARTIFACT_PREFIX@@-generator</module>
        <module>@@MAVEN_ARTIFACT_PREFIX@@-common</module>
    </modules>
    <packaging>pom</packaging>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <parameters>true</parameters>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>public</id>
            <name>aliyun nexus</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>public</id>
            <name>aliyun nexus</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

</project>