---
name: 演示标签
type: 业务
table: biz_demo_tag
module: biz
business: demoTag
perms: biz:demoTag
pattern: 单表
updated: 2026-09-15
---

# 演示标签

> 一句话：这张表在业务上干什么、谁在用、什么场景下会操作它。
> **（待补 —— 这是脚本抽不出来的部分）**

<!-- BEGIN GENERATED: scripts/doc-scan.mjs — 勿手改，跑 `node scripts/doc-scan.mjs` 刷新 -->
<!-- generated-at: 2026-09-15T10:28:30.474Z -->

## 定位

| | |
|---|---|
| 菜单 | æ¼”ç¤ºæ ‡ç­¾（顶级 C 菜单，`order_num=1`） |
| 表 | `biz_demo_tag` — 演示标签表（多对多样板），主键 `tag_id` |
| 路由 | `/biz/demoTag` → `biz/demoTag/index` |
| 权限串 | `biz:demoTag:{list,query,add,edit,remove,export}` |
| 模式 | **单表** |

## 关键字段

> 审计列（del_flag / create_* / update_*）已略去。`*` = 必填。

| 字段 | 类型 | 含义 | 控件 | 字典 | 查询 |
|---|---|---|---|---|---|
| `tag_id` | bigint | 标签ID | `input` |  |  |
| `tag_name` \* | varchar(64) | 标签名称 | `input` |  | LIKE · 常用 |
| `tag_color` | varchar(16) | 标签颜色（仅演示，不参与生成逻辑） | `input` |  | EQ · 常用 |
| `status` | char(1) | 状态（0正常 1停用） | `radio` |  | EQ · 常用 |

> 另有 1 个内容字段不参与筛选：`remark`。

## 代码落点

**后端**

| 层 | 文件 |
|---|---|
| Controller | `backend/@@MAVEN_ARTIFACT_PREFIX@@-biz/src/main/java/@@JAVA_PACKAGE_ROOT@@/biz/controller/BizDemoTagController.java` |
| Domain | `backend/@@MAVEN_ARTIFACT_PREFIX@@-biz/src/main/java/@@JAVA_PACKAGE_ROOT@@/biz/domain/BizDemoTag.java` |
| Service | `backend/@@MAVEN_ARTIFACT_PREFIX@@-biz/src/main/java/@@JAVA_PACKAGE_ROOT@@/biz/service/IBizDemoTagService.java` |
| ServiceImpl | `backend/@@MAVEN_ARTIFACT_PREFIX@@-biz/src/main/java/@@JAVA_PACKAGE_ROOT@@/biz/service/impl/BizDemoTagServiceImpl.java` |
| Mapper 接口 | `backend/@@MAVEN_ARTIFACT_PREFIX@@-biz/src/main/java/@@JAVA_PACKAGE_ROOT@@/biz/mapper/BizDemoTagMapper.java` |
| Mapper XML | `backend/@@MAVEN_ARTIFACT_PREFIX@@-biz/src/main/resources/mapper/biz/BizDemoTagMapper.xml` |

**前端**

| | 文件 |
|---|---|
| 页面 | `frontend/src/views/biz/demoTag/index.vue` |
| API | `frontend/src/api/biz/demoTag.ts` |
| 类型 | `frontend/src/types/api/biz/demoTag.ts` |

类型 barrel `src/types/api/index.ts` 已导出 ✅

## 重建顺序

1. 建表 + 字典：`backend/sql/biz_demo_tag.sql`
2. 菜单 + 权限：⚠️ **没找到含该权限前缀的 SQL 脚本**
3. `mvn -f backend/pom.xml clean install -DskipTests` 并重启后端
4. **重新登录** —— 菜单与权限是登录时缓存进 Redis 的，不重登看不到变化
<!-- END GENERATED -->

## 业务规则

> **（待补）** 字段之间的联动、隐含约束、状态流转、谁有权改什么。
> 只写**读代码看不出来**的；能从上面表格读到的不要重复。

- 待补。

## 设计取舍

> **（待补）** 这个模块里非显然的选择，以及为什么。
> 若是照着生成器默认产物做的、没什么特别的，写一句「无，标准生成产物」即可 ——
> **明确说「没有」比留空有用**，读的人才知道不是忘了写。

- 待补。

## ⚠️ 坑

> **（待补）** 这个模块**特有**的坑。通用坑（生成器、主题、构建）别往这儿塞，
> 写进 `docs/生成器/` 或 `docs/基建/` 对应那篇。

- 待补。
