# Prettier 只管代码，不管别人生成的东西
dist
node_modules
coverage

# 锁文件
pnpm-lock.yaml

# 自动生成的类型声明（路径是实际位置，别想当然写 src/ 下）
auto-imports.d.ts
src/types/components.d.ts

# 含模板占位符，模板态下不是合法 TS（同 eslint.config.js 的排除理由）
vite.config.ts
